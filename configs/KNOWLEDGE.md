# Configuration Knowledge

## Current Contents

This folder contains example configuration files only:

- `models.example.yaml`
- `retrieval.example.yaml`
- `prompts.example.yaml`
- `evaluation.example.yaml`
- `README.md`

## Responsibilities

Configuration files define future local runtime behavior without hardcoding model names, retrieval thresholds, prompt versions, or evaluation settings in application code.

## Update Notes

Update this file when configuration examples are added, renamed, or assigned runtime meaning. Do not document secrets or local-only override values.

