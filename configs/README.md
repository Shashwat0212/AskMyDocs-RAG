# Configuration

This folder will contain shared configuration for models, retrieval, prompts, and evaluation.

Day Zero includes example files only. Do not add runtime-specific secrets here. Local overrides should use ignored files such as `.env` or untracked config copies.

Future implementation should prefer configuration over hardcoded values.
